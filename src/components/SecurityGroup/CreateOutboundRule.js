import React from 'react'

import { Button } from '@material-ui/core';

export default function CreateOutboundRule(props) {
    return (
        <div>
            <Button variant="text" size="small" color="primary" style={{ display: "block" }}>Create Outbound Rule</Button>
        </div>
    )
}
