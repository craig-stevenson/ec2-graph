import React from 'react'

import { Button } from '@material-ui/core';

export default function DeleteInboundRule(props) {
    return (
        <div>
            <Button variant="text" size="small" color="primary" style={{ display: "block" }}>Delete Inbound Rule</Button>
        </div>
    )
}
