import React from 'react'

import { Button } from '@material-ui/core';

export default function CreateInboundRule(props) {
    return (
        <div>
            <Button variant="text" size="small" color="primary" style={{ display: "block" }}>Create Inbound Rule</Button>
        </div>
    )
}
